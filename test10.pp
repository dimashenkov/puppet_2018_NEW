class test_0 {
       $name = $facts['hostname']
              if $name =~ /^dshenkov(\d+)/ {
                                     notice ("TI SI NA HOST $0")
                                                                   }
                                                                                   else {notice ("TI SI NA HOST default")}
                                                                                                   notice ("TI SI NA HOST default")
                                                                                                                                 }
